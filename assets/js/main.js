$(() => {
    console.log('gallery1/main.js => 👍');

    const controller = new Controller();

    controller.activateNav();
    controller.activateBack();
    controller.activateMini();
    controller.activateRotate();
});